<form method="post" action="{{ path('app_back_matiere_delete', {'id': matiere.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');" style="display:inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matiere.id) }}">
    <button class="action-btn delete"><i class="fas fa-trash"></i></button>
</form>
